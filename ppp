dpkg --list | grep linux-image

内核大于等于5.10
apt update -y && apt install sudo -y 
sudo apt install ufw screen unzip -y

sudo ufw allow 22/tcp && 
sudo ufw allow 20000 && 
sudo ufw enable 

mkdir ppp && cd ppp 

带uring的要求内核大于5.10，内存256，其他用兼容版本，aarch为arm CPU
wget https://github.com/liulilittle/openppp2/releases/latest/download/openppp2-linux-amd64-io-uring.zip  
wget https://github.com/liulilittle/openppp2/releases/latest/download/openppp2-linux-amd64.zip 
wget https://github.com/liulilittle/openppp2/releases/latest/download/openppp2-linux-aarch64-io-uring.zip
wget https://github.com/liulilittle/openppp2/releases/latest/download/openppp2-linux-aarch64.zip


unzip openppp2-linux-amd64-io-uring.zip
unzip openppp2-linux-amd64.zip
unzip openppp2-linux-aarch64-io-uring.zip 
unzip openppp2-linux-aarch64.zip

##修改配置文件
chmod +x ppp && ./ppp -m -s


